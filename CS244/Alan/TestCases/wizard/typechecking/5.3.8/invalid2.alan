{ too many arguments for call to }
source invalid2
    function proc(integer i)
    begin
        relax
    end
begin
    call proc(4, 5)
end

{
alanc: invalid2.alan:8:16: error: too many arguments for call to 'proc'
}
