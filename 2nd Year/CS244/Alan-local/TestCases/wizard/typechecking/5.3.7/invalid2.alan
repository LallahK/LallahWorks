{ too few arguments for call to  }
source invalid2
    function proc(integer i)
    begin
        relax
    end
begin
    call proc()
end

{
alanc: invalid2.alan:8:15: error: too few arguments for call to 'proc'
}
